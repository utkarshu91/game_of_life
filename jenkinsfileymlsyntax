pipeline {
   agent {
    node {
      label 'docker-node'
    }
  }
    stages {

        
       
       
        stage('take the values') {
     
            steps {
              script {
               def props = readProperties file: './environment.yml'
               def envi= props["$var_environment"]
               sh " echo ${test.${envi}}"
               }   
                  
        }
 
       
    }

   
}
