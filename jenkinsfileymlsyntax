pipeline {
   agent {
    node {
      label 'docker-node'
    }
  }
    stages {

        
       
       
        stage('take the values') {
     
            steps {
              script {
               def props = readYaml file: './environment.yml'
               def envi= props["$var_environment"]
               echo props.test.$var_environment.toString()
               sh "echo $envi"
               sh " echo ${props.test.${envi}}"
               
               }   
                  
        }
 
       }
    }

   
}
